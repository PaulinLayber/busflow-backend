generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
}

model Horario {
  id Int @id @default(autoincrement())

  empresa String
  origem String
  destino String

  horario String

  tipo String?

  createdAt DateTime @default(now())
}





